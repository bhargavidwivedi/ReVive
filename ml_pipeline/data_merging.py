import pandas as pd\n\n\ndef merge_dataframes(df1, df2, on_col):\n    merged_df = pd.merge(df1, df2, on=on_col, how='inner')\n    return merged_df\n\ndef aggregate_data(df, group_cols, agg_funcs):\n    aggregated_df = df.groupby(group_cols).agg(agg_funcs).reset_index()\n    return aggregated_df\n\ndef data_merging_pipeline(df1, df2, on_col, group_cols, agg_funcs):\n    # Step 1: Merge the dataframes\n    merged_data = merge_dataframes(df1, df2, on_col)\n\n    # Step 2: Aggregate the merged data\n    aggregated_data = aggregate_data(merged_data, group_cols, agg_funcs)\n\n    return aggregated_data\n\nif __name__ == '__main__':\n    # Example usage\n    patient_data = pd.read_csv('patient_data.csv')  # Replace with actual data file\n    readmission_data = pd.read_csv('readmission_data.csv')  # Replace with actual data file\n\n    # Define the column on which to merge\n    on_column = 'patient_id'\n\n    # Define aggregation functions\n    aggregations = { 'age': 'mean', 'readmission_count': 'sum' }\n\n    # Run the data merging pipeline\n    result = data_merging_pipeline(patient_data, readmission_data, on_column, ['age'], aggregations)\n    print(result)\n